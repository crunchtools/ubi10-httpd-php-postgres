name: Gourmand

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  gourmand:
    name: Code Quality (Gourmand)
    uses: crunchtools/gatehouse/.github/workflows/gourmand.yml@v0.9.0
